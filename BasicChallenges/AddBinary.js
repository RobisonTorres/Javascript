console.log("From Code Wars")
console.log()

function addBinary(a,b) {

    /* This function sums two numbers (a, b),
    and return the result in binary as str. */
    return (a + b).toString(2)
}

console.log(addBinary(1, 1)) // Outputs - 10
